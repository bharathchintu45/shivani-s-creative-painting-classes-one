
export interface PricePlan {
  ageRange: string;
  inr: number;
  usd: number;
}

export interface Achievement {
  text: string;
}

export interface LearningItem {
  title: string;
  description?: string;
}
